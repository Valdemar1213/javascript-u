const nome = 'Valdemar'; // String
const nome1 = "Valdemar"; // String
const nome2 = `Valdemar`; // String

const num1 = 10; // Number
const num2 = 10.52; // Number

let nomeAluno; // Undefined = Não aponta para local nenhum na MEMORIA
const sobrenomeAluno = null; // Nulo = Não aponta para local nenhum na MEMORIA

const aprovado = true; // True ou False para Boolean (Lógico)

const a = [1, 2];
const b = a;

console.log(a, b);

b.push(3);
console.log(a, b);